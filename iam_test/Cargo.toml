# Cargo.toml
[package]
name = "iam_test"
version = "0.1.0"
edition = "2021"

[dependencies]
openidconnect = "2.0"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[[bin]]
name = "server"
path = "src/server.rs"